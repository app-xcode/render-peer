{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "PeerJS signaling server for WebRTC",
  "main": "server.js",
  "scripts": {
    "start": "peerjs --port 10000 --path /myapp"
  },
  "dependencies": {
    "peer": "^1.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
